{
  "name": "@zero-agent/lambda-agent",
  "version": "0.1.0",
  "description": "Lambda function for Claude Agent orchestrator",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run package",
    "package": "rm -rf agent-lambda.zip && zip -r agent-lambda.zip dist node_modules",
    "clean": "rm -rf dist agent-lambda.zip"
  },
  "dependencies": {
    "@zero-agent/agent-core": "workspace:*",
    "@anthropic-ai/sdk": "^0.68.0",
    "@aws-sdk/client-lambda": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
