{
  "name": "@zero-agent/lambda-auth",
  "version": "0.1.0",
  "description": "Lambda function for Xero OAuth authentication",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run package",
    "package": "rm -rf auth-lambda.zip && zip -r auth-lambda.zip dist node_modules",
    "clean": "rm -rf dist auth-lambda.zip"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
