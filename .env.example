# Zero Agent - Environment Variables Example
# Copy this file to .env and fill in your values
# DO NOT commit .env to git

# LLM Provider Configuration
LLM_PROVIDER=anthropic              # anthropic | ollama | openai | google | grok
LLM_AUTH_METHOD=api_key             # api_key | oauth | local
LLM_DEFAULT_MODEL=claude-3-5-haiku-20241022

# Anthropic API Key
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Ollama Configuration (for local LLM testing)
# Install from: https://ollama.com
LLM_ENDPOINT=http://localhost:11434

# Database Configuration
DATABASE_PROVIDER=sqlite            # sqlite | dynamodb | postgresql
DATABASE_FILENAME=./data/zero-agent.db  # SQLite only
DATABASE_TABLE_NAME=zero-agent-prod     # DynamoDB only
DATABASE_REGION=ap-southeast-2          # DynamoDB only
# DATABASE_ENDPOINT=http://localhost:8000  # For local DynamoDB testing

# Xero OAuth Credentials
# Get from: https://developer.xero.com/app/manage
XERO_CLIENT_ID=your-client-id
XERO_CLIENT_SECRET=your-client-secret

# Server Configuration (VPS deployment)
NODE_ENV=development                # development | production
PORT=3000                           # Server port
BASE_URL=http://localhost:3000      # Public URL (for OAuth callbacks)
FRONTEND_URL=http://localhost:3000  # Where to redirect after OAuth

# CORS Configuration (production)
# CORS_ORIGIN=https://your-domain.com

# AWS Configuration (legacy - for managed deployment)
# AWS_REGION=ap-southeast-2           # Sydney (optimal for AU/NZ users)
